# ── AI 評測 API 環境變數（複製為 .env 後填入實際值）──────────────
# systemd EnvironmentFile 格式：每行 KEY=VALUE，不要加引號或 export。

# 必填：JWT 簽章密鑰。請用以下指令產生一段隨機字串：
#   openssl rand -hex 32
JWT_SECRET=請改成夠長的隨機字串

# API 監聽埠（需與 Nginx 反向代理設定一致）
PORT=3001

# 資料檔路徑（須為執行服務的使用者可寫，例如 www-data）。
# 實際資料存放於 SQLite，檔案位置是「這個路徑加上 .sqlite3」（例如
# /var/lib/ai-assessment/db.json.sqlite3），DB_PATH 本身不需要跟著改。
# 若偵測到這個路徑存在舊版純 JSON 檔且尚未遷移，開機時會自動讀入遷移，
# 原始 JSON 檔不會被刪除或覆寫，可當作遷移前的備份保留。
DB_PATH=/var/lib/ai-assessment/db.json

# 管理員種子帳號（僅在資料庫尚無管理員時，於首次啟動建立）
ADMIN_EMAIL=admin@rong-rise.com
ADMIN_PASSWORD=請改成強密碼

# production 會啟用 Secure cookie（必須搭配 HTTPS）
NODE_ENV=production

# 反向代理的信任層數（express `trust proxy`，供 req.ip／rate limiter 判斷真實用戶端 IP）。
# 本站架構為 Traefik → Nginx → 這支 API（兩層皆走 loopback，Nginx 會用
# $proxy_add_x_forwarded_for 附加自己的位址），推測應設為 2，但務必於部署後從外部
# 網路實際驗證 req.ip 讀到的是使用者真實 IP，不能只憑推論設定——設太高會讓偽造的
# X-Forwarded-For 被當真、形同繞過限流，設太低則所有請求在限流眼中仍是同一個 IP。
# 本機開發／測試預設 0（不信任任何代理層）。
TRUST_PROXY=0

# ── AI 聊天小幫手（ChatBot）──────────────────────────────────
# 從 https://openrouter.ai 取得 API Key
OPENROUTER_API_KEY=
# 選填：預設 deepseek/deepseek-v4-flash；可換任何 OpenRouter 支援的模型
OPENROUTER_MODEL=deepseek/deepseek-v4-flash
# 選填：傳給 OpenRouter 的 HTTP-Referer，預設正式站網址
APP_URL=https://assess.rong-rise.com
