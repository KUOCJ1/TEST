# 產品開發 Sprint 規劃

> 版本：2026-09　|　對象：產品負責人（CJ哥）、接手開發的工程師
>
> 本文件依據對現有程式碼的盤點（見各 Sprint「現況問題」所引用的檔案）規劃 Sprint。
> 每個 Sprint 約 1–2 週、可獨立上線，順序依「先修正確性 → 再補體驗 → 再擴
> 功能 → 最後還技術債」排列。Sprint 1–5 已全部完成、驗證並上線；Sprint 6
> 是上線後追加的一輪；Sprint 6、7、8 已完成、尚待部署（Sprint 8 為 UI/UX 優化；見各段落背景說明）。架構背景請先讀
> [`PLATFORM_MANUAL.md`](./PLATFORM_MANUAL.md)。

---

## 總覽

| # | Sprint 名稱 | 主軸 | 主要受益者 | 規模 |
|---|---|---|---|---|
| 1 | 報告正確性與作答體驗 | 修正風格型題庫的錯誤呈現、提升作答效度與完成率 | 學員 | S–M |
| 2 | 學員首頁「下一步」導引 | 讓學員一進站就知道現在該做什麼 | 學員 | M |
| 3 | 學習閉環：目標 × 延伸閱讀 × 複測 | 把「看報告」接到「真的去學、再回來測」 | 學員 | M–L |
| 4 | 教練工作台強化 | 催交追蹤、班級學習成效報告、評語效率 | 教練 | M |
| 5 | 品質、無障礙與維運韌性 | 還技術債、補測試、降低維運風險 | 工程/維運 | M |
| 6 | 監控告警、跨梯次分析與自動化提醒 | 故障主動通知、管理者跨班級趨勢、Email 催交 | 工程/維運、管理者 | M |
| 7 | 上線信心與通知擴充 | 部署後可驗證（版本/檢查腳本/連線診斷）、E2E 進 CI、學員通知信 | 工程/維運、學員 | M–L |
| 8 | UI/UX 優化 | 對比度、導覽、首頁、作答體驗、表格、流暢度 | 所有角色 | M–L |

建議順序即為編號順序。Sprint 1 有實際的報告錯誤，應優先處理；Sprint 2、3
可視課程檔期對調；Sprint 5 的部分項目（如 `xlsx` 替換）可穿插在任何 Sprint 中。
Sprint 6 是 Sprint 1–5 全部完成、上線後追加的一輪，時間點是 2026-09（見各
Sprint 段落開頭的日期與 commit 紀錄）。

---

## 驗證標準與推進機制

每個 Sprint 是否「完成、可以往下一個推進」，一律用以下**共同關卡（Gate）**加上
該 Sprint 自己的**功能驗收表**共同判定。兩者都通過才算過關；任一項不過，
該 Sprint 視為未完成，不推進到下一個。

### 共同關卡（每個 Sprint 都要過，缺一不可）

| # | 檢查 | 通過標準 | 指令 |
|---|---|---|---|
| G1 | 前端單元/元件測試 | 全綠，且新增/變更的行為有對應測試（不是只維持原有測試通過） | `npm test` |
| G2 | 後端 API 測試 | 全綠 | `cd server && npm test` |
| G3 | Lint | 0 problems | `npm run lint` |
| G4 | Production build | 成功產出 `dist/`，無 build 期錯誤 | `npm run build` |
| G5 | 後端可正常啟動並回應健康檢查 | `{"ok":true}` | 見下方「本機驗證環境」 |
| G6 | 冒煙測試（Smoke） | 該 Sprint 涉及的畫面至少手動或以 Playwright 跑過一次黃金路徑，無 console error、無明顯版面錯亂（含手機寬度 375px 與深色模式各看一次） | 見各 Sprint 的「冒煙腳本」 |
| G7 | Git 狀態 | 變更已 commit 並 `git push` 到 `claude/ai-assessment-survey-4vhjun`；GitHub Actions `deploy.yml` 綠燈 | `git push` + 確認 PR #7 的 check run 狀態 |
| G8 | 文件同步 | 若該 Sprint 影響架構、API 或使用者操作流程，`PLATFORM_MANUAL.md` / `user-manual.md` 已同步更新 | 人工檢查 diff |

> **關於「部署」**：這個工作環境是雲端沙箱，**無法 SSH 到正式 VPS**（見
> `PLATFORM_MANUAL.md` 第 11 節）。因此本輪自動推進的「部署」定義為：
> ① `npm run build` 產出前端靜態檔、② 後端以正式模式（`NODE_ENV=production`
> 但 `secureCookies` 關閉以便本機 HTTP 測試）在本機啟動、③ 對這組本機「類正式」
> 環境跑 G5、G6。**這不能取代正式站部署**——每個 Sprint 的變更會照常 push、
> 開 PR，但實際上線到 `assess.rong-rise.com` 仍需要你在 VPS 上手動執行
> `CLAUDE.md`／`DEPLOYMENT.md` 的部署指令。每個 Sprint 完成時我會照舊提供
> 那段 VPS 指令。

### 本機驗證環境（G5、G6 共用）

```bash
# 後端：正式模式啟動、資料庫用暫存檔（不動到任何真實資料）
cd server
DB_PATH=/tmp/sprint-verify/db.json JWT_SECRET=sprint-verify-secret \
  ADMIN_EMAIL=admin@verify.local ADMIN_PASSWORD=Verify1234 \
  PORT=4001 NODE_ENV=production node src/server.js &
curl -s localhost:4001/api/health   # 應回 {"ok":true} → G5

# 前端：build 後用 preview 服務靜態檔，模擬正式環境的靜態資源服務
npm run build && npm run preview -- --port 4173
```
每個 Sprint 結束後關閉這兩個背景程序，下一個 Sprint 重新啟動一份乾淨的暫存資料庫，
避免測資跨 Sprint 污染。

### 推進規則

1. 依編號順序（1→2→3→4→5）逐一進行，不並行。
2. 每個 Sprint：實作 → 補測試 → 跑 G1–G6 → 全過才 commit + push（G7）→
   視情況同步文件（G8）→ 記錄驗收結果 → 才開始下一個 Sprint。
3. 若某項功能驗收失敗，先修正並重新跑該 Sprint 的 G1–G6，不允許帶著已知失敗
   項目推進到下一個 Sprint。
4. 待決 backlog（LINE 通知、AI 個人化建議、PostgreSQL 遷移、多租戶、即時告警
   管道）不在目前規劃範圍內，維持「需產品決策後再排入」。Email 通知已於
   Sprint 6 排入，不再屬於待決 backlog（見 Sprint 6 段落與文末待決 Backlog
   表的說明）。

---

## Sprint 1　報告正確性與作答體驗

**目標**：DISC／識己® 這類「風格輪廓型」題庫的所有畫面都不再出現「分數越高
越好」的錯誤暗示；長問卷的作答體驗與效度提升。

### 現況問題

1. **風格型題庫在「我的分析」頁仍用總分做比較**（`dashboard/UserDashboard.jsx`）：
   「學習增益」區塊顯示「課前總分 → 課後總分、整體增益 +N」，「歷次總分趨勢」
   圖也畫總分，「構面進步追蹤」把構面變化標成綠色↑/紅色↓。對 `PROFILE_MODE`
   題庫而言總分沒有意義，構面升降也不是「進步／退步」，而是「風格位移」。
2. **目標面板把最低分風格標成「最待強化」**（`components/GoalPanel.jsx` 使用
   `result.weakest`）：對 DISC 來說，最低分的風格不是缺點。
3. **作答時公開標示反向題**（`components/QuestionCard.jsx` 顯示 🔄 圖示，
   `SurveyApp.jsx` 開頭也提示「標示題目為反向計分」）：反向題的目的是偵測
   「不看題目一律勾高分」的作答傾向，事先告訴受測者哪幾題是反向，等於讓
   這個機制失效，是心理計量上的效度問題。
4. **360° 他評提示文字寫死「領導力行為表現」**（`SurveyApp.jsx`）：評 DISC
   時語意不符。
5. **L9D 90 題一次攤在同一頁**：長問卷完成率與作答品質都會隨疲勞下降。

### 用戶故事與驗收條件

| # | 故事 | 驗收條件 |
|---|---|---|
| 1.1 | 身為 DISC／識己® 學員，我在「我的分析」看到的是**風格變化**而非「進步退步」 | `PROFILE_MODE` 時：隱藏總分增益卡與總分趨勢圖；改顯示「風格變化」卡（課前主要/次要風格 → 課後主要/次要風格，各構面位移用中性色 ←→ 呈現，不用綠紅）|
| 1.2 | 身為風格型題庫學員，目標面板用中性說法引導我 | `PROFILE_MODE` 時文案改為「想刻意練習哪一種風格？」，並可自選構面，而非預設最低分構面 |
| 1.3 | 身為受測者，我看不出哪些題目是反向題 | 作答頁移除反向題標示與說明文字；**計分邏輯不變**；報告／教練端仍可檢視題目設定 |
| 1.4 | 身為 360° 評分者，提示文字符合我正在填的題庫 | 他評提示改從題庫設定讀取（例如新增 `RATER_PROMPT` 欄位，未設定時用中性預設「的日常行為表現」）|
| 1.5 | 身為 L9D 學員，我可以一段一段作答 | 以構面分頁（上一段／下一段），頂部顯示「第 3 / 9 段」與各段完成狀態；最後一段才出現送出鈕；草稿自動儲存機制沿用；題數 ≤ 40 的題庫可維持單頁 |
| 1.6 | 身為學員，我可以用鍵盤快速作答 | 題目聚焦時按 1–5 直接選分並自動跳下一題；符合無障礙焦點樣式 |

### 主要修改檔案
`dashboard/UserDashboard.jsx`、`components/GoalPanel.jsx`、`components/QuestionCard.jsx`、
`SurveyApp.jsx`、`data/assessments/*.js`（`RATER_PROMPT`）、`components/JourneyTimeline.jsx`（確認敘事不引用總分）、
對應測試 `src/test/*`。

### 冒煙腳本（G6）
1. 以 DISC 帳號登入 → 作答並提交 → 「我的分析」：確認**沒有**總分增益卡／總分趨勢圖，改看到「風格變化」卡，構面位移不是綠紅配色。
2. 同一帳號在目標面板新增目標：文案為中性引導語，非「最待強化」。
3. 進 L9D 作答頁：確認題目旁**沒有** 🔄 反向題圖示，且分段導覽（第 N / 9 段）正常運作，跳到未答題會定位到正確分頁。
4. 在 DISC 的 360° 他評頁確認提示文字不是「領導力行為表現」。
5. 手機寬度（375px）與深色模式下重複步驟 1、3，畫面無跑版。

### 風險
- 分頁作答需確保「未答題標紅並跳轉」在跨頁時仍正確（跳到該題所在分頁）。
- 移除反向題標示後，舊版 PDF 手冊若有截圖需一併更新。

---

## Sprint 2　學員首頁「下一步」導引

**目標**：學員登入後的第一個畫面直接告訴他「現在該做什麼」，而不是一排題庫卡片讓他自己判斷。

### 現況問題
- `/home`（`AppShell.jsx` 的 `AssessmentHome`）只列出題庫卡片，學員要自己判斷該做課前、課後、還是 360°。
- 所屬班級的狀態（開放日期、目前階段、還差幾人他評）散落在不同頁面。
- 評完測之後，學員沒有「回來的理由」——首頁沒有顯示目標進度或推薦內容。

### 用戶故事與驗收條件

| # | 故事 | 驗收條件 |
|---|---|---|
| 2.1 | 身為學員，首頁最上方有一張「下一步」卡片 | 依優先序顯示一個主要行動：①所屬班級課前未作答 →「開始課前評測」②課前已完成、班級已進入課後 →「開始課後複測」③360° 他評邀請未完成 →「還有 N 位同事等你評分」④都完成 →「看看你的成長報告」|
| 2.2 | 身為班級學員，我看得到我的班級狀態 | 班級資訊條：班名、教練、目前階段（課前／課後）、課後開放日期倒數 |
| 2.3 | 身為學員，我在首頁看到目標進度 | 「我的目標」摘要：進行中目標數、行動完成率，點擊跳到分析頁目標區 |
| 2.4 | 身為學員，題庫卡片顯示清楚的狀態標籤 | 每張卡片顯示「未作答／課前已完成／課後已完成／可重測」與最近作答日期 |
| 2.5 | 身為首次登入學員，有 3 步驟快速導覽 | 取代現有 `OnboardingBanner` 的一次性說明，可關閉並記住（`localStorage`，失敗時不影響使用）|

### 主要修改檔案
`AppShell.jsx`（抽出 `AssessmentHome` 至 `home/HomePage.jsx`）、`components/AssessmentCard.jsx`、
`components/OnboardingBanner.jsx`、`api/client.js`；後端視需要新增 `GET /api/me/summary`
（一次回傳班級、階段、待評 360°、目標統計，避免首頁打 5 支 API）。

### 冒煙腳本（G6）
1. 尚未作答的學員登入 →`/home` 最上方出現「下一步」卡片，指向「開始課前評測」。
2. 完成課前、班級切到課後階段後重新整理 →「下一步」卡片改為「開始課後複測」。
3. 有待評 360° 邀請的學員登入 → 卡片顯示「還有 N 位同事等你評分」，點擊導向 360° 頁。
4. 題庫卡片檢查狀態標籤（未作答／課前已完成／課後已完成／可重測）與最近作答日期正確。
5. 首次登入帳號看到 3 步驟導覽，關閉後重新整理不再出現。
6. 手機寬度與深色模式下重複步驟 1。

### 風險
- 「下一步」判斷邏輯需與 `SurveyApp.jsx` 的課前/課後自動判定**共用同一個函式**，避免兩處邏輯不一致。

---

## Sprint 3　學習閉環：目標 × 延伸閱讀 × 複測

**目標**：把目前各自獨立的「延伸閱讀」「發展目標」「課後複測」串成一條學習路徑：
**診斷 → 選主題 → 讀 → 做 → 再測**。

### 現況問題
- 延伸閱讀（`components/LearningResources.jsx`）點出去就結束，平台不知道學員讀了什麼。
- 發展目標（`GoalPanel.jsx`）與延伸閱讀沒有關聯，行動項目要學員自己想。
- 沒有任何機制提醒學員「目標設定後 N 週，該回來複測看看有沒有改變」。

### 用戶故事與驗收條件

| # | 故事 | 驗收條件 |
|---|---|---|
| 3.1 | 身為學員，我可以把文章加入「我的學習清單」 | 延伸閱讀每篇文章有「加入清單」按鈕；清單存於後端（新增 `readingList` collection 或併入 `goals`）；可標記「已讀」|
| 3.2 | 身為學員，我可以把一篇文章直接變成目標的行動項目 | 「加入目標」→ 選擇既有目標或新建，行動文字預填「閱讀：〈文章標題〉」並附連結 |
| 3.3 | 身為學員，我有一頁「我的學習」 | 新分頁（或分析頁子區塊）彙整：進行中目標、學習清單（未讀／已讀）、下次建議複測日期 |
| 3.4 | 身為學員，系統建議我何時複測 | 目標建立時可設定「預計檢視日」（預設 4 週後）；到期後首頁「下一步」卡片（Sprint 2）提示「回來複測，看看 ○○ 有沒有改變」|
| 3.5 | 身為學員，複測後我看得到目標構面的變化 | 分析頁目標卡片顯示「設定目標時該構面分數 → 最新分數」（風格型題庫用中性呈現，沿用 Sprint 1 規則）|
| 3.6 | 身為管理者，我看得到延伸閱讀的使用情形 | 管理後台統計：各構面文章點擊數、加入清單數（僅彙總數據，不揭露個人閱讀紀錄）|

### 主要修改檔案
`components/LearningResources.jsx`、`components/GoalPanel.jsx`、新增 `learning/MyLearningPage.jsx`、
`server/src/routes/goals.js`（或新增 `readingList.js`）、`server/src/db.js`（`COLLECTIONS` 加新 collection）、
`admin/AnalyticsTab.jsx`。

### 設計決策（需產品負責人確認，本輪自動推進採用括號內的預設值）
- **閱讀紀錄的隱私**：比照目標的設計（「只有你看得到」）（**預設：教練看不到**，之後如需開放「分享給教練」再加開關）。
- **提醒管道**：本 Sprint 只做**站內提醒**（首頁卡片）。Email 提醒需要 SMTP 服務與寄件網域設定，列入待決 backlog（見文末）。

### 冒煙腳本（G6）
1. 學員在延伸閱讀點「加入清單」→ 進「我的學習」頁看到該篇、狀態未讀；標記已讀後狀態更新。
2. 從一篇文章「加入目標」→ 建立新目標，行動項目預填「閱讀：〈標題〉」與連結。
3. 建立目標時設定預計檢視日為過去日期（測試用）→「下一步」卡片出現複測提醒。
4. 有課前/課後兩筆資料且該構面設有目標的帳號 → 目標卡片顯示「設定時分數 → 最新分數」。
5. 管理後台看得到延伸閱讀點擊數／加入清單數彙總（非個人明細）。

---

## Sprint 4　教練工作台強化

**目標**：讓教練在開課前、課中、課後三個時間點都能快速掌握班級狀態，並大幅
減少撰寫評語的時間。

### 現況問題
- 教練要自己比對成員名單才知道誰還沒交；沒有「一鍵催交」的輔助。
- 班級報告有落點分布與整體評語，但沒有**班級層級的課前 → 課後學習成效**（這是
  對企業客戶交付成果最有說服力的數字）。
- 評語從空白開始寫，同一個班常要重複寫類似內容。

### 用戶故事與驗收條件

| # | 故事 | 驗收條件 |
|---|---|---|
| 4.1 | 身為教練，我一眼看到誰還沒作答 | 班級頁「作答進度」：課前 x/N、課後 y/N，未完成者列表（姓名、Email、加入日期）|
| 4.2 | 身為教練，我可以一鍵產生催交訊息 | 「複製提醒訊息」：自動帶入班名、截止日、報到連結，可直接貼到 LINE／Email |
| 4.3 | 身為教練，我有班級學習成效報告 | 課前 vs 課後：班級平均總分變化、各構面平均變化、進步最多／最少構面、配對樣本人數（只計算前後都有作答者）；可列印／匯出 PDF；風格型題庫改呈現「風格分布變化」|
| 4.4 | 身為教練，我可以用評語範本 | 評語編輯器（`coach/CommentEditor.jsx`）新增「我的範本」：儲存、套用；可一鍵插入學員最強／最弱構面名稱 |
| 4.5 | 身為教練，我可以匯出班級成績 | 匯出 CSV（姓名、階段、總分、各構面分數、落點）；使用純 CSV 產生，不依賴 `xlsx` 套件 |
| 4.6 | 身為教練，我可以比較兩個梯次 | 選同一題庫的兩個班級，並排比較平均雷達圖與落點分布 |

### 主要修改檔案
`coach/GroupWorkspace.jsx`（884 行，建議先拆出 `ProgressPanel`、`GainReport` 子元件）、
`coach/CommentEditor.jsx`、`components/GroupPrintableReport.jsx`、`server/src/routes/coach.js`
（成效報告彙總端點、範本 CRUD）。

### 冒煙腳本（G6）
1. 教練登入 → 班級頁「作答進度」正確顯示課前/課後 x/N 與未完成名單。
2. 點「複製提醒訊息」→ 剪貼簿內容包含班名、截止日、報到連結。
3. 至少一位成員有課前+課後資料的班級 → 成效報告顯示平均變化與配對樣本數；風格型題庫班級顯示風格分布變化而非總分。
4. 儲存一則評語範本 → 套用到另一位學員的評語欄位。
5. 匯出班級 CSV → 檔案可開啟、欄位正確，且不再匯入 `xlsx` 套件。

### 風險
- 成效報告的「配對樣本」邏輯要與 `groupId` 歸屬規則（見 `CLAUDE.md` 班級管理章節）一致，舊資料 `groupId=null` 的相容邏輯需一併考慮。

---

## Sprint 5　品質、無障礙與維運韌性

**目標**：償還已知技術債，讓系統在交接之後仍然容易維護、不容易壞。

### 工作項目與驗收條件

| # | 項目 | 驗收條件 |
|---|---|---|
| 5.1 | 移除 `xlsx` 高風險相依（`npm audit` 唯一 high） | 批次匯入改用維護中的替代方案（如 `exceljs`，或改為僅支援 CSV + 範本說明）；`npm audit --omit=dev` 0 high |
| 5.2 | E2E 黃金路徑測試（Playwright 已安裝未使用） | 至少涵蓋：註冊→作答→看報告、教練建班→產生 QR→學員掃碼加入、管理者開關題庫；可在 CI 執行（**事後更正**：三條路徑本機可跑，但 CI 其實沒接上，當時判定過關太寬鬆，移至 Sprint 7.4）|
| 5.3 | `chat.js` 補後端測試 | 比照 `learningResources` 測試，以 `t.mock.method(globalThis, 'fetch')` 覆蓋：未設 key 回 503、401/402/429 錯誤轉換、串流正常路徑 |
| 5.4 | 無障礙檢查 | 以 axe 掃描主要頁面 0 critical；所有互動元件可純鍵盤操作；圖表提供文字替代（雷達圖附構面分數表）|
| 5.5 | 深色模式完整度 | 盤點仍使用寫死顏色（如 `bg-emerald-50`、`bg-amber-50`、`text-red-600`）的區塊，補上 `.dark` 覆寫或改用語意化 token |
| 5.6 | 拆分超大元件 | `PrintableReport.jsx`（1119 行）、`GroupWorkspace.jsx`（884 行）拆成可獨立測試的子元件，行為不變 |
| 5.7 | 自動備份 | `deploy/` 新增備份腳本與 cron 範例（`sqlite3 .backup` + 保留最近 14 份），寫入 `DEPLOYMENT.md` |
| 5.8 | 外部依賴監控 | `GET /api/health` 擴充為可選的 `?deep=1`，回報第二大腦 API 與 OpenRouter 可達性，供外部監控服務輪詢 |

### 主要修改檔案
`admin/BatchUploadSection.jsx`、`package.json`、`e2e/`（新增）、`server/test/`、
`components/PrintableReport.jsx`、`coach/GroupWorkspace.jsx`、`src/index.css`、`deploy/`、`DEPLOYMENT.md`。

### 冒煙腳本（G6）
1. `npx playwright test` 三條黃金路徑全過。
2. `npm audit --omit=dev` 無 high/critical。
3. 批次匯入頁上傳一份範本 Excel，成功解析並匯入。
4. axe 掃描 `/home`、作答頁、報告頁、教練後台、管理後台 0 critical。

---

## Sprint 6　監控告警、跨梯次分析與自動化提醒

**背景**：Sprint 3 當時把「Email 通知」列為待決 backlog（見文末），理由是
「需要選定寄信服務、寄件網域設定、費用決策」；VPS 部署驗證時 `npm audit`
也發現後端正式環境相依有 3 個中風險漏洞（已在 Sprint 5 結束後單獨修復並
push，見對應 commit，非本 Sprint 工作項目，這裡僅記錄延續性）。本 Sprint
把「Email 通知」正式從 backlog 排入，同時處理兩項已知維運缺口：深度健康
檢查沒人主動盯、管理者看不到跨班級/跨梯次的趨勢。

**目標**：讓系統壞掉時第一時間有人知道（而不是等學員或教練回報）、讓管理者
能看到「這門課辦了好幾梯，整體是變好還變差」、讓催交從「複製貼上」變成
「按一下真的寄出去」。

### 現況問題
- `GET /api/health?deep=1`（Sprint 5 產出）存在，但沒有任何東西定期呼叫它；
  第二大腦或 OpenRouter 掛掉，要等有人主動發現才知道。
- 管理後台「整體統計」（`AnalyticsTab.jsx`）把**所有班級的所有提交攤平成一張
  總表**，看不出「按班級」的維度，更看不出同一套題庫辦了好幾梯之後，
  平均表現是進步還是退步——這個數字對企業客戶續約/擴大合作最有說服力，
  但目前只有教練層級的「比較梯次」（`CohortCompare.jsx`）且一次只能比較
  自己名下的 2 個班級。
- 催交目前只有「複製提醒訊息」（`ProgressPanel.jsx`）——教練仍要自己貼去
  LINE 或 Email 才會真的送達；系統完全沒有寄信能力（`.env.example` 沒有任何
  SMTP 相關設定）。

### 用戶故事與驗收條件

| # | 故事 | 驗收條件 |
|---|---|---|
| 6.1 | 身為維運者，系統故障時我會主動收到通知，而不用自己去查 | 後端常駐一個內部排程（預設每 5 分鐘，`HEALTH_CHECK_INTERVAL_MINUTES` 可調），呼叫 `deepHealthCheck()`；第二大腦或 OpenRouter 從「正常」變「異常」時寄一封告警信到 `ADMIN_EMAIL`；從「異常」恢復「正常」時也寄一封「已恢復」信。只在**狀態變化**時寄信，不會每次檢查都寄（否則信箱會被灌爆）|
| 6.2 | 身為維運者，重啟服務不會讓我漏收或多收告警 | 上一次已知狀態存進資料庫（新增輕量 collection 或欄位），服務重啟後讀回來繼續比對，不會因為重啟就誤判成「狀態變化」而多寄一次 |
| 6.3 | 身為管理者，我可以看到某套題庫辦過的所有班級，依開課時間排序比較 | 管理後台「整體統計」新增「跨班級／跨梯次」區塊：列出所選題庫底下全部班級（不限特定教練)，每班的作答人數、平均總分、平均達成率、開課日期；沒設定開課日的班排最後並標示「尚未設定開課日」|
| 6.4 | 身為管理者，我可以一眼看出這門課是進步還是退步 | 同一區塊附趨勢圖：X 軸為班級（依開課日排序），Y 軸為平均達成率／風格型題庫改顯示風格分布世代比較（沿用 Sprint 1「不暗示分數高低」的規則）|
| 6.5 | 身為教練，我可以直接寄出催交提醒信，不必自己複製貼上 | 「作答進度」面板新增「寄送提醒信」按鈕：對該班「本階段未完成」的成員逐一寄送個人化提醒信（含班名、階段、報到連結）；同一班 1 小時內只能寄一次（避免誤觸洗版），按鈕在冷卻中顯示剩餘時間 |
| 6.6 | 身為系統管理者，沒設定 SMTP 時其他功能不受影響 | 未設定 SMTP 環境變數時，6.1／6.5 的寄信動作優雅降級（回傳明確錯誤訊息／按鈕顯示「尚未設定寄信服務」），不影響評測本身；比照 `chat.js` 對 `OPENROUTER_API_KEY` 未設定的既有處理模式 |

### 主要修改檔案
`server/src/lib/mailer.js`（新增，包 `nodemailer`）、`server/src/lib/health.js`（`checkAndAlert()` 狀態比對＋告警）、
`server/src/server.js`（排程）、`server/src/db.js`（新增 `systemStatus` collection）、
`server/src/routes/coach.js`（新增 `POST /groups/:id/remind`，`groups` 加 `lastReminderSentAt`）、
`coach/ProgressPanel.jsx`（寄信按鈕）、`coach/GroupOverviewSection.jsx`、
`admin/CohortTrendSection.jsx`（新增）、`admin/AnalyticsTab.jsx`、`utils/analytics.js`（`computeCohortTrend()`）、
`server/.env.example`。

### 實作結果與規劃差異
- **6.3 不需要改後端**：管理後台本來就透過 `GET /api/coach/groups` 拿到全站班級
  （admin 通過該路由的教練篩選），直接傳給 `AnalyticsTab` 即可，`admin.js` 沒動。
- **6.3/6.4 加了「課前／課後」切換**：規劃時沒想到——每人「最新一筆」若課前課後混著取，
  會把「進來時的程度」跟「上完課的成果」攪在同一個平均裡，各梯就沒得比。
- **順手修了 Sprint 5 的迴歸**：拆分 `GroupWorkspace` 時漏搬了「作答進度」面板
  （Sprint 4 的催交追蹤），正式站因此看不到這個面板。本 Sprint 補回並加了能抓到
  這種漏搬的迴歸測試（`group-overview-section.test.jsx`）。
- **順手修了深色模式分頁對比**：分頁列「選取中」膠囊（`bg-white text-brass-600`）在
  深色模式只有約 2:1 對比、比未選取還暗；Sprint 5 的深色稽核漏掉了，影響管理後台、
  教練後台、班級工作台三處既有分頁列。以 `src/index.css` 的複合選擇器一次修正。
- **冒煙驗證**：以本機 SMTP 收信器實測——第二大腦斷線/恢復各只收到一封信（約 5 輪檢查
  期間）、重啟服務後不會多寄；催交信只寄給課前未完成者＋待加入者、內容個人化、
  1 小時冷卻生效；跨梯次比較在亮色/深色/手機寬度與 axe 掃描皆正常。

### 設計決策（需產品負責人確認，本輪自動推進採用括號內的預設值）
- **寄信服務**：不綁定特定廠商，採 `nodemailer` + 通用 SMTP 環境變數
  （`SMTP_HOST`／`SMTP_PORT`／`SMTP_USER`／`SMTP_PASS`／`SMTP_FROM`），可接
  Gmail、SendGrid、Mailgun 等任何提供 SMTP relay 的服務（**預設：先用你現有
  的任何一個信箱的 SMTP 設定即可跑起來，之後要換成正式寄件網域/SPF-DKIM
  再換**，這屬於 VPS 端 `.env` 設定，不是程式碼決策，我沒辦法從這裡幫你選
  或申請）。
- **告警管道**：復用同一組寄信機制，寄給 `ADMIN_EMAIL`（**預設：不另外接
  Slack/Discord webhook**；如果之後想要即時性更高的告警管道，可以再開一個
  獨立的 Sprint 補上，不影響本 Sprint 範圍）。
- **催交信 vs LINE**：本 Sprint 只做 Email（**預設：保留「複製提醒訊息」
  按鈕原樣，新按鈕是額外選項而非取代**，LINE 官方帳號整合維持待決 backlog）。
- **跨梯次排序依據**：以 `group.startDate` 排序（**預設**）；管理者若想自訂
  排序（例如依教練分組），列入本 Sprint 結束後的下一輪待辦。

### 冒煙腳本（G6）
1. 手動把 `BRAIN_API_BASE_URL` 指向一個不存在的位址、確認排程跑過一次後
   `ADMIN_EMAIL` 收到「異常」告警信；改回正確位址後收到「已恢復」信。
2. 重啟後端服務，確認**不會**因為重啟就誤發一次多餘的告警信。
3. 管理後台選一套至少有 2 個班級（不同開課日）的題庫，跨班級區塊正確列出
   兩班且依開課日排序；趨勢圖顯示兩個資料點。
4. 教練後台「作答進度」點「寄送提醒信」，未完成成員的信箱各收到一封個人化
   提醒信；1 小時內再按一次，按鈕顯示冷卻中且不會重複寄送。
5. 移除/清空 SMTP 環境變數後重跑，確認 6.1、6.5 都優雅降級、不影響其他
   功能與既有測試。

### 風險
- 寄信是本系統第一次呼叫外部 SMTP 服務，需要在 VPS 上實際驗證：防火牆是否
  擋 SMTP 埠（25/465/587）、寄出的信會不會被收件方判定為垃圾信（沒有
  SPF/DKIM 的情況下，Gmail 等大型信箱有一定機率直接丟垃圾桶）——這部分
  本機沙箱無法完整模擬，需要你在 VPS 部署後實際寄一封測試信確認有沒有
  被擋。
- 內部排程（6.1）是常駐在 Node process 裡的 `setInterval`，systemd 重啟
  服務時排程會跟著重新啟動——這是預期行為，但代表如果服務本身頻繁重啟
  （例如部署腳本每次都重啟），排程的計時會跟著歸零，不是「絕對每 5 分鐘」
  而是「距離上次啟動 5 分鐘」，可接受但值得知道。

---

## Sprint 7　上線信心與通知擴充

**背景**：Sprint 6 完成後，下一步是**跟產品負責人一起在 VPS 上部署並實測**。回顧
前幾輪部署，驗證卡在幾個地方：確認「正式站跑的是不是新版」要靠比對打包檔名、
`TRUST_PROXY` 只能口頭提醒「請從外部驗證」卻沒有工具、部署後的檢查清單散在
三份文件裡。另外誠實記錄兩個缺口：Sprint 5 驗收條件 5.2 寫了「可在 CI 執行」，
實際上 CI（`deploy.yml`）從沒跑過 Playwright，當時判定過關太寬鬆；Sprint 5 拆元件
時漏搬「作答進度」面板，E2E 也沒抓到，因為沒有任何 E2E 走到「有作答資料的班級總覽」。

**目標**：讓部署後「新版有沒有上去、設定對不對」可以在一分鐘內用工具確認，而不是
靠經驗判讀；把 E2E 真的接進 CI；並用 Sprint 6 已經建好的寄信基礎，低成本補上學員端
的通知。

**建議順序**：7.1–7.3 先做——它們就是我們一起部署時要用的工具，做完再部署，
當次部署就能直接用上。

### 用戶故事與驗收條件

| # | 故事 | 驗收條件 |
|---|---|---|
| 7.1 | 身為維運者，我一眼知道正式站跑的是哪一版 | `deploy.sh` 建置時寫入 commit SHA 與建置時間；`GET /api/health` 回傳 `version: { commit, builtAt }`；管理後台頁尾顯示前端版本。部署後 `curl /api/health` 的 commit 與 `git rev-parse --short HEAD` 一致；前後端版本不一致時管理後台顯示警示（代表只更新了一半）|
| 7.2 | 身為維運者，部署完一個指令就知道有沒有問題 | 新增 `deploy/verify.sh`：健康檢查、深度檢查、版本比對、首頁 200、SMTP 是否設定、備份 cron 是否存在、最近一份備份是否在 26 小時內——逐項印 PASS／WARN／FAIL；`deploy.sh` 結尾自動執行（WARN 不中斷部署）|
| 7.3 | 身為管理者，我不用懂反向代理也能確認 `TRUST_PROXY` 設對了 | 管理後台新增「連線診斷」卡片（`GET /api/admin/diagnostics/connection`，僅 admin）：顯示「伺服器看到你的 IP」、`X-Forwarded-For` 原始值、目前 `trust proxy` 層數，並提示「請和 whatismyip 之類網站顯示的 IP 比對；若顯示 127.0.0.1 或伺服器內網位址，代表 TRUST_PROXY 設太低」|
| 7.4 | 身為開發者，E2E 真的在 CI 上擋住壞掉的版本 | `deploy.yml` 加 Playwright job（安裝 Chromium、跑 `npm run test:e2e`），失敗時不部署預覽站；新增第 4 條黃金路徑：教練開啟「有作答資料」的班級總覽，斷言作答進度、KPI、成員比較表都在（能抓到 Sprint 5 那種漏搬）|
| 7.5 | 身為學員，到了預計檢視日我會收到複測提醒信 | 目標的 `reviewDate` 到期時寄一封提醒信（每個目標只寄一次，`reviewReminderSentAt`），信裡附「回來複測」連結；每日排程，沿用 Sprint 6 的 `lib/mailer.js` 與排程模式；未設定 SMTP 時靜默略過 |
| 7.6 | 身為學員，教練給我新評語時我會收到通知 | 教練新增或更新評語後寄一封通知信（只說「有新評語」、附連結，**不在信裡放評語內容**）；同一份作答 1 小時內多次修改只寄一次 |
| 7.7 | 身為學員，我可以關掉通知信 | 個人設定新增「Email 通知」開關（預設開啟），關掉後 7.5、7.6 都不寄；催交信（Sprint 6）屬課程行政通知，不受此開關影響，設定頁寫明這一點 |
| 7.8 | 身為管理者，風格型題庫的整體統計不暗示高低 | 管理後台「數據分析」上方 KPI 對 DISC／識己® 不顯示「平均達成率」（Sprint 6 調查時發現的既有不一致，跨梯次區塊已經處理，扁平 KPI 還沒）|

### 主要修改檔案
`deploy/deploy.sh`、`deploy/verify.sh`（新增）、`server/src/app.js`（health 版本資訊）、
`server/src/routes/admin.js`（連線診斷）、`admin/AdminDashboard.jsx`、`admin/AnalyticsTab.jsx`、
`.github/workflows/deploy.yml`、`e2e/`（新增第 4 條）、`server/src/lib/`（複測提醒排程）、
`server/src/routes/goals.js`、`server/src/routes/submissions.js`（評語通知）、
`server/src/routes/auth.js`（通知偏好）、`profile/`（通知開關）。

### 實作結果與規劃差異
- **7.3 做成一整個「系統狀態」分頁**（`GET /api/admin/system-status`），而不只是連線診斷卡片：
  版本、外部依賴、寄信設定、告警排程最後執行時間放在一起，部署時一頁看完。
- **7.4 CI 第一次跑就失敗，但不是 E2E 的問題**：`app-flow` 測試等待 lazy 載入的管理後台，
  CI 冷啟動編譯超過 Testing Library 預設 1 秒。修正後 CI 實際跑完 4 條黃金路徑（31 秒）才
  建置部署。另加 `AUTH_RATE_LIMIT`（僅 E2E），解決測試共用後端時自己把登入額度用光的老問題。
- **7.7 沒有新增開關，而是接上既有的兩個**：個人設定早就有「評測提醒」「教練評語通知」
  兩個開關，但沒有任何程式讀它們（說明文字還寫著「實際發送需搭配通知管道」）——等於
  讓使用者操作一個假開關。本 Sprint 讓它們真正控制 7.5、7.6，並改寫說明。
- **順手修的問題**：
  - 教練可以對**別班**學員留評語（只檢查身分是教練）；7.6 上線後還會因此寄信給對方學員。
    改成只能對自己班上的學員留評語，管理者不受限。
  - `PATCH /auth/profile` 以前把前端送來的 `preferences` 整包合併存檔，改為白名單。
  - 個人設定頁的姓名、密碼、預設評量欄位沒有和標籤關聯（axe 2 個 critical），補上。
    這頁過去從沒被無障礙掃描涵蓋——Sprint 8 會把掃描擴大到所有頁面。
  - 風格型題庫的 KPI 文字值（「🔥 魅力領導者」）套用數字用的 3xl 字級擠爆卡片，改小。
- **冒煙驗證**：`verify.sh` 在假 VPS 目錄結構下分別跑「正常」「部署一半＋設定缺漏」
  「服務掛掉」三種情境，PASS/WARN/FAIL 與結束代碼皆正確；本機 SMTP 收信器實測複測提醒只寄
  給未關閉通知者、同一目標不重寄，評語連存兩次只寄一封且信裡沒有評語內容；系統狀態分頁
  在亮色/深色/手機寬度與 axe 掃描皆正常。

### 設計決策（需產品負責人確認，本輪自動推進採用括號內的預設值）
- **學員通知的同意方式**：（**預設：開啟，可在個人設定關閉**）。複測提醒與評語通知
  都跟學員自己的課程直接相關，屬服務性通知；仍提供關閉開關。若企業客戶要求改成
  預設關閉、學員自行開啟，只要改預設值。
- **評語通知的內容**：（**預設：信裡不放評語內容**）只說「教練給了你新評語」並附連結
  ——評語可能包含敏感的績效觀察，Email 可能被轉寄或出現在共用信箱。
- **版本號來源**：（**預設：`git rev-parse --short HEAD`，由 `deploy.sh` 在建置時寫入**）
  VPS 本來就是用 git checkout 部署，不另外維護版本號。
- **CI 跑 E2E 的時間成本**：（**預設：接受**）約多 2–3 分鐘；換來每次 push 都驗過三條
  黃金路徑。

### 冒煙腳本（G6）
1. 本機跑 `deploy.sh` 的建置段落 → `curl /api/health` 回傳的 commit 與目前 HEAD 一致；
   刻意只重建前端不重啟後端 → 管理後台出現「前後端版本不一致」警示。
2. `deploy/verify.sh` 在「全部正常」與「SMTP 未設定、沒有備份 cron」兩種情境下
   分別印出正確的 PASS／WARN。
3. 管理後台「連線診斷」在本機顯示 127.0.0.1 並出現「TRUST_PROXY 可能設太低」提示；
   用帶 `X-Forwarded-For` 的請求搭配 `TRUST_PROXY=1` 時顯示該位址。
4. push 後 GitHub Actions 的 Playwright job 綠燈；故意把 `ProgressPanel` 從總覽拿掉
   的分支上，第 4 條黃金路徑會失敗。
5. 把某個目標的 `reviewDate` 設成昨天 → 排程跑過後本機 SMTP 收信器收到一封複測提醒，
   再跑一次不會重寄；關掉通知開關的帳號收不到。
6. 教練連續修改同一份評語三次 → 學員只收到一封通知，信裡沒有評語內容。
7. 管理後台選 DISC → 扁平 KPI 不再出現「平均達成率」。

### 風險
- 7.4：GitHub Actions 的 Chromium 版本跟本機沙箱不同，第一次接上可能需要調整
  `playwright.config.js` 的啟動參數；E2E 若偶發不穩（flaky），要修測試而不是加重試了事。
- 7.5／7.6：這是系統第一次**主動寄信給學員**（Sprint 6 的催交信是教練手動觸發）。
  寄件網域若沒有 SPF/DKIM，學員端進垃圾信匣的比例會比寄給管理者更高——建議在
  開啟 7.5／7.6 之前先完成寄件網域設定。
- 7.3 顯示的是「這次請求」伺服器看到的 IP，只能驗證**當下**的設定；之後若反向代理
  架構改變，仍要重新檢查一次（`verify.sh` 會提醒）。

---

## Sprint 8　UI/UX 優化：更專業、更直覺、更流暢

**背景**：產品負責人希望整體介面「更專業、更直覺、更 smooth」。為了讓這三個形容詞可以
驗收，先做一次全站稽核：用真實感的種子資料（兩梯班級、課前/課後、L9D／DISC 結果、目標、
評語），以學員／教練／管理者三種角色走過 21 個畫面，每個畫面各拍桌機亮色、手機 390px、
桌機深色，並跑 axe（critical＋serious）、量版面位移（CLS）與水平溢出。

### 稽核發現（依影響排序）
- **文字對比不足是全站性的**：每一頁都有 axe serious `color-contrast`，單頁最多 84 處。
  根因集中在少數色票——次要文字用的 `slate-400`（#b3a48c，對白底只有 **2.4:1**）與
  `slate-500`（3.6–4.0:1），另有 `ink-50`、`brass-500`、狀態色 `emerald/amber-600`。WCAG AA
  要 4.5:1。Sprint 5 的無障礙關卡只擋 critical，所以一直沒被抓到。
- **深色模式的米色提示框**（`bg-brass-50`：新手引導、摘要框等）維持亮底，但框內文字被深色
  模式提亮了，對比約 2:1——淺字配淺底。
- **真實 bug**：教練後台「成員比較」總分差距顯示 `+5.700000000000003`（浮點數沒有四捨五入）。
- **表格與按鈕被擠到斷字**：成員比較表的姓名逐字直排（「林／佳／蓉」）、落點徽章折三行；
  首頁評量卡片按鈕「重新作／答」「查看分／析」、狀態標籤「進行／中」斷在字中間。
- **桌機導覽列折成兩行**：教練、管理者的分頁比學員多，1280px 寬就放不下。手機底部導覽
  管理者有 7 個圖示，也過擠。
- **首頁資訊堆疊**：主要內容前面疊了 4 個橫幅（新手引導、下一步、班級狀態、目標）；新手
  引導在已經做過 4 次評測的帳號上還在顯示。
- **作答頁（使用時間最長的畫面）在手機上**：李克特量表 4 個選項一排、「5」單獨掉到第二排
  變成全寬按鈕，標籤被擠成「非常不同／意」三行碎字。
- **AI 小幫手浮動按鈕蓋住內容**：作答頁蓋住選項「4」、首頁蓋住「查看」連結。
- **無障礙 critical**：手機版教練後台有沒有名稱的圖示按鈕（×3）、熱力圖捲動區無法用鍵盤
  聚焦、班級設定與管理後台班別頁有 6 個欄位沒有關聯標籤。
- **每一頁的分頁標題都是同一個「全方位職能評測」**：多開分頁或看瀏覽紀錄時無從分辨。
- **細節**：DISC 構面名稱重複（「D 支配型 支配型」）、延伸閱讀統計顯示原始代碼
  （`ai-competency`、`workflow`）、報告內的「雷達圖與構面／強弱項與建議」看起來像普通文字
  而不像分頁；未登入的訪客每次開首頁，console 都有兩個 401 紅字。
- 表現良好的部分：各頁 CLS ≤ 0.025、手機寬度沒有任何水平溢出、深色模式整體結構完整。

### 工作項目與驗收條件

| # | 項目 | 驗收條件 |
|---|---|---|
| 8.1 | 色彩對比達 WCAG AA | 從色票層級修正（次要文字色、狀態色、深色模式米色框），不逐頁補丁；稽核的所有畫面在**亮色與深色**下 axe `color-contrast` 皆 0；次要／三級文字仍保有明顯層次 |
| 8.2 | 導覽列不折行 | 桌機 ≥1024px 所有角色導覽列單行；「個人設定」收進使用者選單；手機底部導覽最多 6 項 |
| 8.3 | 首頁減少堆疊 | 完成過任一評測後不再顯示新手引導；班級狀態與目標摘要合併成一條精簡資訊列 |
| 8.4 | 評量卡片與按鈕不斷字 | 按鈕、徽章、表頭一律不在字中間換行；卡片改為「一個主要動作＋次要文字連結」，已完成改用狀態標示而非停用按鈕 |
| 8.5 | 作答體驗 | 李克特量表在所有寬度都是等寬 5 格一排、觸控目標 ≥44px；手機只在兩端顯示「非常不同意／非常同意」；選完一題自動平滑捲到下一題未答題（尊重「減少動態效果」設定） |
| 8.6 | 浮動按鈕不遮內容 | 作答頁隱藏 AI 小幫手按鈕；其他頁預留底部空間，不再蓋住可點擊元素 |
| 8.7 | 教練表格與無障礙 critical | 修正浮點數顯示；成員比較表在窄欄位也不逐字直排；所有圖示按鈕有名稱、可捲動區可鍵盤聚焦、所有欄位有關聯標籤——全站 axe critical 0 |
| 8.8 | 每頁有自己的標題 | 例如「我的分析｜全方位職能評測」，切換分頁時更新 |
| 8.9 | 流暢度 | 換頁時捲回頂端並有輕微淡入（尊重 `prefers-reduced-motion`）；按鈕有按下回饋 |
| 8.10 | 細節修正 | DISC 構面名稱不重複；延伸閱讀統計顯示題庫與構面中文名稱；報告內分頁有明確的分頁樣式；未登入訪客 console 不再出現 401 |

### 主要修改檔案
`tailwind.config.js`、`src/index.css`、`AppShell.jsx`、`components/AssessmentCard.jsx`、`components/QuestionCard.jsx`、
`SurveyApp.jsx`、`components/ChatBot.jsx`、`coach/GroupOverviewSection.jsx`、`components/DimensionHeatmap.jsx`、
`components/ResultPanel.jsx`、`admin/LearningResourceStatsPanel.jsx`、`server/src/routes/auth.js` 等。

### 設計決策（本輪自動推進採用括號內的預設值）
- **調深次要文字色而不是改用更大字級**：（**預設：調整 `slate-400/500` 色票本身**）全站幾百處
  用到這兩個 class，改色票一次到位，也避免各頁各自補丁造成不一致。代價是整體會比現在
  「深一點」——這正是可讀性要的。
- **深色模式米色框改成深色版本**：（**預設：改**）深色模式下出現大片亮米色區塊本來就刺眼，
  改成暖深色底＋提亮文字，跟其他區塊一致。
- **作答頁自動捲到下一題**：（**預設：開啟**）90 題的 L9D 能明顯減少手動捲動；系統設定
  「減少動態效果」的使用者改為瞬間定位。

### 冒煙腳本（G6）
重跑 Sprint 8 稽核爬蟲（同一組種子資料、21 個畫面 × 桌機／手機／深色），與稽核前比較：
axe critical＋serious 全數歸零、無水平溢出、CLS 不變差；逐一檢視截圖確認上述每一項。

### 實作結果與規劃差異

**稽核前後對照**（同一組種子資料，21 個畫面 × 桌機亮色／手機 390px／桌機深色 = 63 張）：

| 指標 | 稽核前 | 完成後 |
|---|---|---|
| axe serious（幾乎都是 `color-contrast`） | 每一頁都有，單頁最多 84 處 | **0** |
| axe critical | 手機教練後台、班級設定、管理後台班別頁共 10 處 | **0** |
| 瀏覽器 console 錯誤（未登入首頁 401） | 每次 2 個 | **0** |
| 水平溢出 | 0 | 0 |
| CLS 最大值 | 0.025 | 0.048（L9D 手機作答頁，見下） |
| 分頁標題 | 全站同一個 | 每頁各自 |

- **8.1 對比**：照規劃從 token 層修（`slate-400/500`、`ink-50`、`brass-500`、`emerald-600`、
  `amber-600`）。規劃時沒料到的是**題庫設定裡的 hex 色**（構面色、落點色）也大量不過 AA——
  那些是設定資料不能直接改，改成新增 `utils/color.js`：徽章底色用 `badgeBg()` 自動加深、
  文字色用 `dimTextStyle()` 算出淺／深色模式各一個合格色。深色模式的狀態色提亮規則被
  Tailwind 改寫壞掉，另開 `src/styles/dark-status.css`（原因見 `PLATFORM_MANUAL.md` 第 6 節
  「共用介面規範」）。
- **8.2 導覽**：個人設定、使用說明、手冊下載、登出一起收進新的 `UserMenu`（規劃只提到個人
  設定；其他三顆也在頁首擠位置，一併收掉）。管理者手機底部導覽 7 → 6 項。
- **8.3 首頁**：新手引導只在沒有任何作答時顯示；班級狀態條與目標摘要改成同一列並排
  （手機上下排）。沒有真的合併成單一元件——兩者資料來源與點擊目的地不同，並排已解決
  「疊四層」的問題。
- **8.5 作答**：自動捲動只在**第一次**回答某題時觸發（改答不跳），目的地是「下一個未作答」
  而不是單純下一題；鍵盤作答時焦點一起移動。
- **8.6 浮動按鈕**：作答頁隱藏，提交後出現並帶入這次的結果當對話脈絡。
- **8.10 未登入 401**：新增 `GET /api/auth/session`（未登入回 200 `{user:null}`），`/auth/me`
  維持原本 401 語意，不影響其他呼叫者。
- **CLS 0.048 比稽核前的 0.025 高**：來源是 L9D 手機作答頁的 lazy 題庫載入，仍在 Google
  「良好」門檻 0.1 以下，列為觀察項目，本輪不處理。
- **E2E 順帶變快**：加了自動平滑捲動後，學員黃金路徑一度要 37 秒。追查後發現
  `playwright.config.js` 頂層的 `use.reducedMotion` 根本沒生效，改用
  `contextOptions.reducedMotion` 後降到約 15 秒。
- **Gate**：lint 0、前端 271／271、後端 125／125、build 成功、E2E 4／4。新增測試：
  `color.test.js`、`user-menu.test.jsx`、`question-card.test.jsx`、成員比較四捨五入回歸測試、
  `/auth/session` 後端測試。

---

## 待決 Backlog（需產品決策後再排入）

| 項目 | 為什麼還不能直接做 |
|---|---|
| LINE 通知 | 需要 LINE 官方帳號與 Messaging API 設定；台灣企業學員的實際觸及率可能高於 Email |
| AI 個人化學習建議 | 用 OpenRouter 依報告結果生成客製學習計畫；需確認每次呼叫的成本上限與內容審核方式 |
| 資料庫升級 PostgreSQL | 目前規模 SQLite 足夠；當同時在線人數或多機部署需求出現時再評估 |
| 企業客戶多租戶（組織層級報表） | 牽涉資料模型調整（新增 `organization`），需先確認商業模式 |
| 即時告警管道（Slack/Discord webhook） | Sprint 6 先用 Email 告警；若需要更即時的管道再排入 |

> Email 通知已於 Sprint 6 排入並實作（催交提醒、健康檢查告警）；複測提醒、
> 教練新評語通知已排入 Sprint 7（7.5–7.7），沿用 Sprint 6 的 `lib/mailer.js`。

---

## 每個 Sprint 的完成定義（Definition of Done）

- `npm run lint` 0 problems、`npm test` 與 `cd server && npm test` 全綠、`npm run build` 成功。
- 新功能有對應測試；修正的錯誤有能重現原問題的回歸測試。
- 深色模式與手機寬度（375px）手動檢查過。
- `docs/PLATFORM_MANUAL.md` 與 `docs/user-manual.md` 若受影響需同步更新。
- 推送後在 VPS 執行部署並以 `curl localhost:3101/api/health` 驗證。
