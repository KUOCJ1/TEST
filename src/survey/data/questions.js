// Re-exports from ai-competency assessment config for backward compatibility.
export {
  SCALE_MIN,
  SCALE_MAX,
  SCALE_LABELS,
  DIMENSIONS,
  ALL_QUESTIONS,
  TOTAL_QUESTIONS,
  MIN_SCORE as MIN_TOTAL_SCORE,
  MAX_SCORE as MAX_TOTAL_SCORE,
} from './assessments/ai-competency.js';
